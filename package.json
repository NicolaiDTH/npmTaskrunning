{
  "name": "dice_simulator_2015",
  "version": "1.0.0",
  "description": "Dice simulators. All the rage.",
  "scripts": {
    "test": "mocha",
    "uglify": "uglifyjs src/models/* src/frontend.js  -m -c -o build/app.js",
    "copy-files": "cp src/*.html build/ & cp src/*.css build/",
    "build": "npm run copy-files && npm run uglify"
  },
  "author": "Nicolai DTH",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^2.2.5",
    "uglify-js": "^2.4.23"
  }
}
